"use server";

export const startTraining = async () => {
  throw new Error("Training is temporarily unavailable during API migration.");
};

export const trainingReply = async () => {
  throw new Error("Training is temporarily unavailable during API migration.");
};

export const completeTraining = async () => {
  throw new Error("Training is temporarily unavailable during API migration.");
};

export const trainingHistory = async () => {
  return [];
};